[project]
name = "ryan-tool"
version = "1.0.1"
description = "Add your description here"
requires-python = ">=3.13"
dependencies = [
    "database_errors"
]

[tool.uv.workspace]
members = [
    "src/database_errors",
]

[tool.uv.sources]
database_errors = { workspace = true }
